{
  "version": 2,
  "routes": [
    // 优先匹配图片文件
    { "src": "/map.png", "dest": "/map.png" },
    // 匹配视频目录
    { "src": "/videos/(.*)", "dest": "/videos/$1" },
    // 其他所有请求返回index.html
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
